"use strict";
/*
 * Copyright Elasticsearch B.V. and/or licensed to Elasticsearch B.V. under one
 * or more contributor license agreements. Licensed under the Elastic License;
 * you may not use this file except in compliance with the Elastic License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
const url_1 = require("url");
const is_internal_url_1 = require("./is_internal_url");
function parseNext(href, basePath = '') {
    const { query, hash } = url_1.parse(href, true);
    if (!query.next) {
        return `${basePath}/`;
    }
    let next;
    if (Array.isArray(query.next) && query.next.length > 0) {
        next = query.next[0];
    }
    else {
        next = query.next;
    }
    // validate that `next` is not attempting a redirect to somewhere
    // outside of this Kibana install.
    if (!is_internal_url_1.isInternalURL(next, basePath)) {
        return `${basePath}/`;
    }
    return next + (hash || '');
}
exports.parseNext = parseNext;
